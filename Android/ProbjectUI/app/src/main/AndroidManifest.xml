<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ucc.probjectui">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homeViewActivity" />
        <activity android:name=".homeControlActivity" />
        <activity android:name=".webViewActivity" />
        <activity android:name=".SQLActivity"></activity>
    </application>

    <!--<activity-->
        <!--android:name="com.example.helloworld.DisplayMessageActivityWebView"-->
        <!--android:label="@string/title_activity_display_message_webview"-->
        <!--android:parentActivityName="com.example.myfirstapp.MainActivity" >-->
        <!--<meta-data-->
            <!--android:name="android.support.PARENT_ACTIVITY"-->
            <!--android:value="com.example.myfirstapp.MainActivity" />-->
    <!--</activity>-->

</manifest>